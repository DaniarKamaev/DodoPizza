<Window x:Class="PizzaProject.ChekBasketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PizzaProject"
        mc:Ignorable="d"
        Title="Корзина" Height="450" Width="800"
        MinWidth="600" MinHeight="400">
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.Row="0" VerticalAlignment="Top" Margin="0,0,10,0">
            <Button Content="Назад" Click="BackClick" 
                    Height="30" Margin="0,0,0,10" Padding="10,0"/>
            
            <Button Content="Оформить заказ" 
                    Height="45" 
                    Background="Orange" 
                    FontWeight="Bold"
                    Padding="10,0"/>
        </StackPanel>

        <ListBox x:Name="ItemBox" Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="0,0,0,1" 
                            Padding="10,5" Margin="0,0,0,5">
                        <StackPanel>
                            <TextBlock Text="{Binding PizzaName}" 
                                      FontWeight="Bold" 
                                      FontSize="16"
                                      TextWrapping="Wrap"/>
                            
                            <Grid Margin="0,5,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Price, StringFormat='Цена: {0} руб.'}" 
                                              FontSize="12"/>
                                    <TextBlock Text="{Binding Grams, StringFormat='Вес: {0}г'}" 
                                              FontSize="12"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Calories, StringFormat='Калории: {0}'}" 
                                              FontSize="12"/>
                                    <TextBlock Text="{Binding Quantity, StringFormat='Количество: {0}'}" 
                                              FontSize="12"/>
                                </StackPanel>
                            </Grid>
                            
                            <TextBlock Text="{Binding AddedDate, StringFormat='Добавлено: {0:dd.MM.yyyy HH:mm}'}" 
                                      FontSize="10" 
                                      Foreground="Gray"
                                      Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,5"/>
                    <Setter Property="Background" Value="Transparent"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <Border Grid.Column="1" Grid.Row="1" 
                Background="LightYellow" 
                Padding="10,5" 
                Margin="0,10,0,0"
                BorderBrush="Orange" 
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="TotalTextBlock" 
                          Grid.Column="0"
                          Text="Итого: " 
                          FontWeight="Bold"/>
                          
                <TextBlock x:Name="TotalPriceTextBlock" 
                          Grid.Column="1"
                          Text="0 руб." 
                          FontWeight="Bold" 
                          Foreground="Red"/>
            </Grid>
        </Border>
    </Grid>
</Window>